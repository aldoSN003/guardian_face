import Navbar from "../../components/Navbar";

export default function AdminSettingsPage() {
    return (
        <div>
            <Navbar role="admin" />
            <h2>Admin Settings</h2>
            <p>Change password and other admin settings here.</p>
        </div>
    );
}
